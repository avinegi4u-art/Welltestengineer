---
description: DST PTA Pro single-file app — feature, bug-fix, and refactor contract
globs:
  - dst-pressure-transient-analysis*.html
  - dst-pta*.html
---

# DST Pressure Transient Analyzer Pro

Baseline app: `dst-pressure-transient-analysis-pro-v2.11.html` (local-first, offline, single HTML/JS file).

## New feature

Add this feature to my DST/PTA app. Keep it local-first, single-file HTML/JavaScript, and preserve all existing behavior. Make the feature engineering-grade, transparent, and visible in the plots and summary panels. Do not break existing import, QC, Horner, derivative, or report logic.

When Horner, derivative, boundary, composite, or uncertainty logic changes, surface results in plots and summary panels.

## Bug fix

Fix this bug in my DST/PTA app with the smallest safe change. Keep the offline/local-first workflow intact and do not break any existing calculations, plots, or imports. First identify the root cause, then patch it and verify the affected workflow still works.

Check: UI events, CSV/JSON parsing, date/time ordering, unit conversion, plotting, calculation consistency.

## Refactor

Refactor this DST pressure transient analysis app to make the code cleaner, more modular, and easier to extend, but do not change the user-facing behavior. Keep it as a single-file HTML/JavaScript app unless there is a strong reason not to. Split logic into readable functions, reduce duplication, standardize naming, and keep calculations traceable for engineering review. Preserve all outputs, plots, and gating logic. If you improve structure, make sure the app still behaves exactly the same from the user's perspective.
