{
  "name": "FlowSim Pro",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd flowsim-pro/backend && ./.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
      "description": "FlowSim Pro FastAPI backend on http://localhost:8000 (seeds a sample case on first boot)."
    },
    {
      "name": "frontend",
      "command": "cd flowsim-pro/frontend && npm run dev",
      "description": "FlowSim Pro Next.js frontend on http://localhost:3000 (proxies /api to the backend)."
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 },
    { "name": "backend", "port": 8000 }
  ]
}
