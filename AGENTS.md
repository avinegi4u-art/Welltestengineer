# AGENTS.md

## Cursor Cloud specific instructions

This repository ("Welltestengineer") is currently an empty scaffold with only a `README.md`. There are no source code files, dependencies, build tools, tests, or services to run.

- **No package manager or dependencies** — no `package.json`, `requirements.txt`, `Cargo.toml`, or similar files exist.
- **No application code** — no source files of any language are present.
- **No tests or linting** — no test frameworks or lint configurations are set up.
- **No services** — no backend, frontend, or database services to start.

When code is added to this repository, this file should be updated with relevant development setup instructions, service descriptions, and testing guidance.
